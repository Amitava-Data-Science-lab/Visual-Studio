# API
# Docker Postgres runs on port 5433 (5432 is reserved for local Postgres)
DATABASE_URL=postgresql+psycopg://vsb:vsb@localhost:5433/vsb
JWT_SECRET=change-me

# Development mode (use ORM metadata for schema creation)
# Production should use: alembic upgrade head
DEV_MODE=true

# Builder UI
VITE_API_BASE_URL=http://localhost:8000

# Runtime UI
VITE_RUNTIME_API_BASE_URL=http://localhost:8000
